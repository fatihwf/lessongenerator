kind = "web"
previewPath = "/"
title = "Bloom Lesson Generator"
version = "1.0.0"
id = "artifacts/bloom-frontend"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23480

[services.development]
run = "pnpm --filter @workspace/bloom-frontend run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/bloom-frontend", "run", "build" ]
publicDir = "artifacts/bloom-frontend/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23480"
BASE_PATH = "/"
